```bash
npx openapi-typescript https://demo.uissh.com/docs/schema/?format=json --output src/requests/schema.ts
```

```bash
npm install -g vite@latest
```

```bash
# Build the app for production
npm run build

# Build the app for Django
vite build --base=/static/common/
```